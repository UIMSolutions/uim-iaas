apiVersion: v1
kind: Service
metadata:
  name: objectstore-service
  namespace: objectstore
  labels:
    app: objectstore
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: objectstore
